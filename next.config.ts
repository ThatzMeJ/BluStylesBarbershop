import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'cdn.prod.website-files.com'],
  },
  /* config options here */
};

export default nextConfig;